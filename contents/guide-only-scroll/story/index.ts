import type { ScrollEnginePublic } from "@handscroll/core";

/** Weak pack: no click/flyTo state machine. Commentary is hotspot `openPanel` + guide chapters. */
export function registerStory(_engine: ScrollEnginePublic): void {
  // Intentionally empty.
}
